// import database connection
const db = require('./server.js');

const cTable = require('console.table');

// helper functions for viewing all employees, roles, and departments


// helper functions for adding employees, roles, and departments
class department {
  constructor(id, name) {
    this.id = id;
    this.name = name;
  }
  

}
